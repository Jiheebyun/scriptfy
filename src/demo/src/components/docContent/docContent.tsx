import React from "react";


const DocContent = () => {

    // 해당 컴포넌트의 기능 
    // 해당 하는 내용의  title UI 
    // 컴포넌트의 import  설명 UI
    // Basic 기능 설명 UI

    // 같은 기능의 부수적인 기능이 결합된 기능 설명 UI

    return (
        <div>

        </div>
    )
}